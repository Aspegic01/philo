#ifndef PHILO_H
#define PHILO_H

#include <unistd.h>
#include <stdio.h>
#include <stdlib.h>
#include <limits.h>
long	ft_atol(const char *nptr);
void	exit_error(char *error);
long	check_arg(int ac , char **av);
#endif
